{
    "name": "remote-connect-app",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend && node server.js",
        "build": "cd frontend && npm install && npm run build",
        "dev": "concurrently \"cd backend && node server.js\" \"cd frontend && npm run dev\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}
